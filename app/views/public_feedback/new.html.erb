<div class="min-h-screen bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-2xl mx-auto">
    <div class="text-center mb-8">
      <h2 class="text-3xl font-extrabold text-gray-900">
        Share Anonymous Feedback
      </h2>
      <p class="mt-2 text-sm text-gray-600">
        Your feedback is completely anonymous. We don't track who submits what.
      </p>
    </div>

    <div class="bg-white shadow sm:rounded-lg">
      <div class="px-4 py-5 sm:p-6">
        <%= form_with model: @feedback_response, url: public_feedback_path(@feedback_request.token), method: :post, data: { turbo: false }, class: "space-y-6" do |f| %>
          <% @feedback_request.questions.each_with_index do |question, index| %>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">
                <%= question['text'] %>
              </label>
              <% if index < 3 %>
                <%= text_area_tag "feedback_response[question_#{question['id']}]", nil,
                    class: "shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md",
                    rows: 3,
                    placeholder: "Your feedback..." %>
              <% end %>
            </div>
          <% end %>

          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Overall Feedback (Required)
            </label>
            <%= f.text_area :content,
                class: "shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md",
                rows: 6,
                placeholder: "Share your overall thoughts and feedback... (minimum 50 characters)",
                required: true %>
            <p class="mt-2 text-sm text-gray-500">
              Minimum 50 characters required
            </p>
          </div>

          <div class="flex justify-end">
            <%= f.submit "Submit Anonymous Feedback",
                class: "inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
          </div>
        <% end %>
      </div>
    </div>

    <div class="mt-6 text-center text-sm text-gray-500">
      <p>🔒 Your feedback is anonymous. We delete all identifying metadata after 24 hours.</p>
    </div>
  </div>
</div>
